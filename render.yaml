services:
  - type: web
    name: ws-media-stream-server
    runtime: node
    nodeVersion: "20"
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
